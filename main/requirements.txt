# Voice Conversation System - Dependencies

# Audio I/O
sounddevice>=0.4.6
soundfile>=0.12.1
numpy>=1.24.0
# 读 WAV 由 ai_client 内对 torchaudio.load 的 soundfile 补丁处理，通常无需 torchcodec/系统 FFmpeg

# OpenAI API key from .env (optional)
python-dotenv>=1.0.0

# OpenAI API (audio input support)
openai>=1.30.0

# Coqui TTS - local voice cloning with XTTS-v2
# Requires Python 3.9-3.11 and PyTorch
# Note: this will install PyTorch as a dependency (~2 GB)
TTS>=0.22.0
# XTTS 依赖旧版 transformers API（BeamSearchScorer）；勿用 transformers 5.x，否则会 ImportError
transformers>=4.38,<4.46

# Optional: MP3 playback support (requires ffmpeg on system)
pydub>=0.25.1

# Optional: generate test audio slots (needs internet)
gTTS>=2.3.0
