# Voice Conversation — 核心依赖（小 GPT + 播放 + 串口等）
# 默认 TTS 为本地 Coqui：请再执行  pip install -r requirements-coqui.txt
# 若只用 ElevenLabs、不装 Coqui，可把 config 里 TTS_BACKEND 设为 elevenlabs 并配好 API。

# Audio I/O
sounddevice>=0.4.6
soundfile>=0.12.1
numpy==1.22.0; python_version <= "3.10"
numpy>=1.24.0; python_version >= "3.11"

python-dotenv>=1.0.0
certifi>=2024.2.2
openai>=1.30.0

# 小 GPT 问句生成（QuestionStructureGenerator）
sentencepiece>=0.2.0
torch>=2.0.0

# MP3 回放（ElevenLabs 返回 MPEG）；系统需安装 ffmpeg
pydub>=0.25.1

pyserial>=3.5

# Optional: setup_slots 等脚本
gTTS>=2.3.0
