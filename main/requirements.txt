# Voice Conversation — 默认路径：ElevenLabs TTS + 本地小 GPT 问句（不含 Coqui）
# Intel Mac 上若装整套 Coqui（TTS），会拖入 librosa→numba→llvmlite，常需本地编译 LLVM。
# 仅在用「本地克隆声」时再安装：pip install -r requirements-coqui.txt

# Audio I/O
sounddevice>=0.4.6
soundfile>=0.12.1
numpy==1.22.0; python_version <= "3.10"
numpy>=1.24.0; python_version >= "3.11"

python-dotenv>=1.0.0
certifi>=2024.2.2
openai>=1.30.0

# 小 GPT 问句生成（QuestionStructureGenerator）
sentencepiece>=0.2.0
torch>=2.0.0

# MP3 回放（ElevenLabs 返回 MPEG）；系统需安装 ffmpeg
pydub>=0.25.1

pyserial>=3.5

# Optional: setup_slots 等脚本
gTTS>=2.3.0
